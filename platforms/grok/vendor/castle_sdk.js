'use strict';

// Castle's SDK chunk is build-specific and is intentionally not vendored here.
// grok_protocol.py discovers the current lazy-loaded chunk from accounts.x.ai,
// validates it, and caches it under data/grok/castle at runtime.
